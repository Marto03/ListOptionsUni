<UserControl
    x:Class="ListsOptionsUI.Views.UserDetailsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:c="clr-namespace:ListsOptionsUI.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="clr-namespace:ListsOptionsUI.Helpers"
    xmlns:local="clr-namespace:ListsOptionsUI.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:ListsOptionsUI.ViewModels"
    mc:Ignorable="d">
    <UserControl.Resources>
        <c:StringToVisibilityConverter x:Key="StringToVisibilityConverter" />
        <c:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" IsEnabled="{Binding IsManagePanelVisible, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=True}">

            <!--  Списък с потребители  -->


            <ListView ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedUser}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn DisplayMemberBinding="{Binding UserName}" Header="Username" />
                        <GridViewColumn DisplayMemberBinding="{Binding Type}" Header="Type" />
                        <GridViewColumn>
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button
                                            Command="{Binding DataContext.ConfigureUserCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Content="Manage" />
                                        <Button
                                            Command="{Binding DataContext.DeleteUserCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Content="Delete" />
                                    </StackPanel>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>

            <Button Content="Добави нов потребител" />
        </StackPanel>
        <Expander
            Grid.Row="3"
            Margin="10"
            Header="Добавяне на нов потребител"
            IsExpanded="{Binding IsExpanderOpen}">
            <StackPanel Margin="10" Orientation="Vertical">
                <TextBox Margin="0,5" Text="{Binding NewUser.UserName, UpdateSourceTrigger=PropertyChanged}" />
                <PasswordBox
                    Margin="0,5,0,10"
                    helpers:PasswordBoxHelper.BindPassword="True"
                    helpers:PasswordBoxHelper.BoundPassword="{Binding NewUser.Password, Mode=TwoWay}" />
                <ComboBox
                    Margin="0,5"
                    ItemsSource="{Binding UserTypes}"
                    SelectedItem="{Binding NewUser.Type}" />
                <Button
                    Margin="0,5"
                    Command="{Binding SaveNewUserCommand}"
                    Content="💾 Запази" />
            </StackPanel>
        </Expander>
    </Grid>
</UserControl>
