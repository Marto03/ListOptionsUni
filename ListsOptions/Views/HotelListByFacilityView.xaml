<UserControl
    x:Class="ListsOptionsUI.Views.HotelListByFacilityView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ListsOptionsUI.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ComboBox
            Grid.Row="0"
            Margin="0,0,0,10"
            DisplayMemberPath="Name"
            ItemsSource="{Binding Facilities}"
            SelectedItem="{Binding SelectedFacility}" />

        <Button
            Grid.Row="1"
            Margin="0,0,0,10"
            Command="{Binding ShowHotelsCommand}"
            Content="Покажи" />

        <ListBox Grid.Row="2" ItemsSource="{Binding HotelsWithSelectedFacility}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
