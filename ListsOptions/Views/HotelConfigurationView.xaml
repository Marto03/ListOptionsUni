<UserControl
    x:Class="ListsOptionsUI.Views.HotelConfigurationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:c="clr-namespace:ListsOptionsUI.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="clr-namespace:ListsOptionsUI.Helpers"
    xmlns:local="clr-namespace:ListsOptionsUI.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <c:HotelToIsCheckedMultiConverter x:Key="HotelToIsCheckedMultiConverter" />
    </UserControl.Resources>

    <StackPanel Margin="20">
        <TextBlock
            Margin="0,0,0,10"
            FontSize="16"
            FontWeight="Bold"
            Text="Избери хотел:" />

        <ListBox ItemsSource="{Binding Hotels}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <RadioButton
                        Command="{Binding DataContext.SelectHotelCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding}"
                        Content="{Binding Name}"
                        GroupName="Hotels">
                        <RadioButton.IsChecked>
                            <MultiBinding Converter="{StaticResource HotelToIsCheckedMultiConverter}" Mode="OneWay">
                                <!--  SelectedHotel от ViewModel  -->
                                <Binding Path="DataContext.SelectedHotel" RelativeSource="{RelativeSource AncestorType=UserControl}" />
                                <Binding />
                            </MultiBinding>
                        </RadioButton.IsChecked>
                    </RadioButton>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>




        <StackPanel Margin="0,20,0,10" Orientation="Horizontal">
            <TextBox
                Width="200"
                helpers:PlaceholderHelper.PlaceholderText="Нов хотел..."
                Text="{Binding NewHotelName}" />
            <Button
                Margin="10,0,0,0"
                Command="{Binding AddHotelCommand}"
                Content="Добави" />
        </StackPanel>

        <Button
            Width="200"
            Command="{Binding SaveHotelCommand}"
            Content="Запази избора" />
    </StackPanel>
</UserControl>
